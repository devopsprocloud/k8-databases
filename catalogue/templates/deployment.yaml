
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogue
  namespace: roboshop 
  labels:
    app: catalogue
    project: roboshop 
    tier: app
spec:
  # modify replicas according to your case
  replicas: {{ .Values.deployment.replicas }}
  selector: # replica labels 
    matchLabels:
      app: catalogue
      project: roboshop 
      tier: app
  template: # pod-definition
    metadata:
      labels:
        app: catalogue
        project: roboshop 
        tier: app
    spec:
      containers:
      - name: catalogue
        image: "devopsprocloud/catalogue:{{ .Values.deployment.imageVersion }}"
        imagePullPolicy: Always 
        resources: 
          requests: 
            cpu: "50m"
            memory: "128Mi"
          limits: 
            cpu: "100m"
            memory: "256Mi"
        envFrom: 
        - configMapRef: 
            name: catalogue 